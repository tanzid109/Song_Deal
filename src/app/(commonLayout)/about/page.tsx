import AboutSection from '@/Homepage/About/AboutSection';
import React from 'react';

const page = () => {
    return (
        <div>
            <AboutSection/>
        </div>
    );
};

export default page;